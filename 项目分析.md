# 项目分析

## 结论

这是一个基于 `Next.js 16 + React 19 + TypeScript + Supabase` 的轻量 CRM，已经具备登录、客户管理、沟通记录、用户管理、飞书多维表格同步和 AI 字段提取能力。

当前项目可以继续开发，但原始版本存在明显安全和交付风险，已先完成第一轮整理与安全改造。

## 已完成整理

- 项目根目录已从 `CRM/projects/` 调整为 `CRM/`
- 保留原有源码结构：`src/`、`scripts/`、`public/`
- 新增 [.env.example](/Users/leileigwl/Code/Daily/20260515/codex/code/cmh/CRM/.env.example) 方便接手配置

## 技术结构

- 前端：Next.js App Router、React 19、Tailwind v4、shadcn/ui
- 后端：Next.js Route Handlers
- 数据层：Supabase + Drizzle schema
- 外部集成：飞书多维表格、Coze LLM SDK

核心目录：

- [src/app](/Users/leileigwl/Code/Daily/20260515/codex/code/cmh/CRM/src/app)
- [src/lib](/Users/leileigwl/Code/Daily/20260515/codex/code/cmh/CRM/src/lib)
- [src/storage/database/shared/schema.ts](/Users/leileigwl/Code/Daily/20260515/codex/code/cmh/CRM/src/storage/database/shared/schema.ts)
- [scripts](/Users/leileigwl/Code/Daily/20260515/codex/code/cmh/CRM/scripts)

## 业务能力

- 登录/登出/注册
- 客户列表、创建、删除、详情
- 客户沟通记录
- 用户管理
- 飞书多维表格初始化与同步
- AI 文本提取客户字段

## 已修复问题

### 1. 飞书密钥硬编码

原项目在代码中写死 `FEISHU_APP_ID` 与 `FEISHU_APP_SECRET`，现在已改为只允许从环境变量读取。

### 2. 弱密码哈希

原项目使用固定盐 `SHA-256`。现在已改成基于 Node `crypto.scrypt` 的带随机盐哈希格式：

`scrypt:<salt>:<hash>`

并保留旧密码格式兼容，用户成功登录后会自动升级为新格式。

### 3. 可伪造 Session

原项目 session 只是 base64 JSON。现在已改为：

`base64url(payload).hmac_signature`

依赖 `SESSION_SECRET` 校验签名，无法伪造。

### 4. 默认管理员账号

原项目初始化脚本写死默认管理员邮箱和密码。现在改为必须通过环境变量传入：

- `INIT_ADMIN_EMAIL`
- `INIT_ADMIN_PASSWORD`
- `INIT_ADMIN_NAME`

## 仍需继续处理

### 1. 数据库迁移不完整

[schema.ts](/Users/leileigwl/Code/Daily/20260515/codex/code/cmh/CRM/src/storage/database/shared/schema.ts) 里仍有“手动 ALTER TABLE”痕迹，说明迁移体系还没补齐。

建议：

- 补 `drizzle` migration
- 把 `feishu_record_id` 和索引正式纳入迁移

### 2. 客户编号生成有并发冲突风险

[src/app/api/customers/route.ts](/Users/leileigwl/Code/Daily/20260515/codex/code/cmh/CRM/src/app/api/customers/route.ts) 目前按总数 `count + 1` 生成编码，并发下会撞号。

建议：

- 改数据库 sequence
- 或改成日期 + 随机短码

### 3. 飞书同步缺少重试机制

当前创建客户后直接异步同步飞书，失败后没有任务补偿。

建议：

- 增加 `sync_status`
- 使用后台任务或定时补偿

### 4. AI 接口缺少限流与超时治理

当前 AI 提取接口能用，但缺少用户级限流、超时和失败治理。

## 建议接手顺序

1. 先补 `.env` 与数据库迁移
2. 再修客户编号生成
3. 再补飞书同步重试
4. 最后做 UI/流程优化
